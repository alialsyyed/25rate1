# Database Configuration (for full-stack deployment only)
DATABASE_URL=your_postgresql_connection_string

# Session Secret (for full-stack deployment only)
SESSION_SECRET=your_session_secret_key

# Development/Production Mode
NODE_ENV=production

# Note: This project is configured for static deployment to Netlify
# The backend functionality requires a separate server deployment
